apiVersion: v1
kind: Service
metadata:
  name: pm-chatbot-api-service
  namespace: pm-chatbot
  labels:
    app: pm-chatbot
    component: api
spec:
  selector:
    app: pm-chatbot
    component: api
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP
  type: ClusterIP 